# Generated by pnpm - reformatting it would desync `--frozen-lockfile` installs.
pnpm-lock.yaml

# Build output. Also covered by .gitignore, which Prettier honours by default,
# but listed here so a local build never trips `pnpm lint`.
dist/
