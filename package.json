{
  "name": "caido-vigolium",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vigolium plugin for Caido",
  "scripts": {
    "build": "pnpm -r build && node scripts/pack.js",
    "clean": "node scripts/clean.js",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  },
  "packageManager": "pnpm@10.33.2"
}
